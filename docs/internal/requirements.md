<!-- FilePath: docs/internal/requirements.md -->

# Simple Voice — Requirements

This document records every requirement the owner has stated for Simple Voice. It is the
source of truth for scope: a feature is done only when it satisfies the matching entry here.

## 1. Product

| ID  | Requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P-1 | The app is called **Simple Voice**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| P-2 | It is a Tauri 2 desktop app living in this repository (`yuyudhan/simple-voice`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| P-3 | It is built for other people to use, not only the owner: no hard-coded personal paths, keys, or vocabulary.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| P-4 | macOS first and only for now (Apple Silicon, macOS 14+; Apple Speech requires macOS 26+).                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| P-5 | Installation and updates happen only through the install script `scripts/install.sh`, published with every GitHub release at `releases/latest/download/install.sh`, which installs from the GitHub release with checksum and code signature verified, into `~/Applications` so that no administrator password is ever needed. The repo carries the script and the release automation that publishes it.                                                                                                                                                    |
| P-6 | The UI must be elegant, simple and functional, in the product's own design language, Signal (`docs/internal/design.md`); it must not imitate another product's visual identity.                                                                                                                                                                                                                                                                                                                                                                            |
| P-7 | Everything the owner's Hammerspoon dictation module does today must be done by this app instead (see § 3).                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| P-8 | Users learn about and install new releases without leaving the app: it checks for a newer release on launch and once a day (automatic checks can be turned off; "Check now" is always available), then shows a banner that can be dismissed for that version, an "Update Available" item in the menu bar and app menu, and an Install update button that runs the published install script (P-5) in the background; the script installs from the GitHub release, quits, replaces and reopens the app, and a failure is shown in the app with the log path. |

## 2. Storage

| ID  | Requirement                                                                                                                                                                         |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S-1 | A local SQLite database stores all user configuration, the dictation history, and the personal dictionary.                                                                          |
| S-2 | Default location: `~/.simplevoice/` (database file `~/.simplevoice/simple-voice.db`).                                                                                               |
| S-3 | The database location can be changed in Settings. Changing it moves the database; the app remembers the location across restarts.                                                   |
| S-4 | On first launch the app creates the directory and the database and applies the schema without any manual step.                                                                      |
| S-5 | New app versions upgrade an existing database in place through versioned, forward-only migrations, taking a backup before migrating. Old data is never lost on upgrade.             |
| S-6 | Every SQL statement is checked at compile time (sqlx query macros with a committed offline cache).                                                                                  |
| S-7 | Opening a database written by a newer app version (a downgrade) is refused with a clear message rather than risking the data.                                                       |
| S-8 | API keys (Groq, custom endpoint) are stored in the local database (file mode 0600) with the rest of the configuration; `GROQ_API_KEY` in the environment is honoured as a fallback. |

## 3. Dictation (parity with the Hammerspoon module)

| ID   | Requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-1  | System-wide dictation: record from the microphone, transcribe, paste into the focused input of any app.                                                                                                                                                                                                                                                                                                                                                       |
| D-2  | Two global shortcuts, both configurable in Settings: **hold to speak** (recording stops on release; default the Fn key alone) and **toggle to speak** (press to start, press again to stop; default `Ctrl+/`). Fn used as a modifier (Fn+Arrow, Fn+Delete) never starts or toggles a dictation. While Fn is a shortcut and Accessibility access is missing, the X-3 warning also appears next to the shortcut settings and names the Fn shortcuts it affects. |
| D-3  | Esc cancels a recording in progress without transcribing.                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-4  | Prefer the built-in microphone by default (Bluetooth headsets switch to low-quality HFP); the microphone is selectable in Settings.                                                                                                                                                                                                                                                                                                                           |
| D-5  | Languages: English, Hindi and Hinglish by default; Hinglish stays romanised, pure Hindi comes out in Devanagari. A detection outside the allowed languages is re-run pinned to the fallback language. Allowed languages are configurable.                                                                                                                                                                                                                     |
| D-6  | Personal dictionary words bias recognition (Whisper `prompt`, capped at 850 characters) and are passed to the formatter as preferred spellings.                                                                                                                                                                                                                                                                                                               |
| D-7  | Replacement rules (`heard -> written`) are applied deterministically, longest phrase first, whole words, case-insensitive.                                                                                                                                                                                                                                                                                                                                    |
| D-8  | Deterministic formatting runs first: rules, internal-caps brand casing (`argocd` → `ArgoCD`), no space before punctuation, sentence capitalisation, standalone `i` → `I`.                                                                                                                                                                                                                                                                                     |
| D-9  | An LLM formatting pass (Groq `qwen/qwen3.8-27b`, reasoning off, temperature 0) then rewrites the text: drops fillers and self-corrections, keeps Hinglish word for word, writes shortcuts as `Ctrl+Shift+M`, uses conventional tech spellings, and decides the layout itself (prose, `- ` bullets, `1. ` steps). Dictated instructions or questions are formatted, never answered.                                                                            |
| D-10 | The formatting pass must stay fast (~0.25 s). If it fails, times out (2.5 s + 5 ms/word), is truncated, is empty, or grows beyond 1.5× the input + 10 words, the deterministic text is pasted and marked "unformatted". Transcripts under 3 words skip it. It can be turned off.                                                                                                                                                                              |
| D-11 | Overlapping dictations never paste out of order and never share an audio file: each recording has an id and its own file; an older result arriving after a newer paste is dropped (kept in history).                                                                                                                                                                                                                                                          |
| D-12 | Audio cues on start and stop.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-13 | A watchdog stops and transcribes after a maximum recording length (default 5 minutes).                                                                                                                                                                                                                                                                                                                                                                        |
| D-14 | A failed dictation keeps its audio so it can be retried from history; a dictation is never silently lost.                                                                                                                                                                                                                                                                                                                                                     |
| D-15 | The dictated text is placed on the clipboard and stays there by default, so it is never lost when pasting fails; a setting restores the previous clipboard after a successful paste instead. A dictation that could not be pasted is stored in history as "not pasted" with the reason, never as pasted.                                                                                                                                                      |
| D-16 | Pasted text ends in whitespace so back-to-back dictations never run together: a space is appended unless the text already ends in whitespace (for example a trailing newline). History and the clipboard copy of a retry keep the text without it.                                                                                                                                                                                                            |

## 4. Transcription engines and models

| ID  | Requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| M-1 | **Groq** cloud Whisper (`whisper-large-v3-turbo`) with the user's API key.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| M-2 | **Parakeet** local models, as in FluidVoice: Parakeet TDT v3 (multilingual), Parakeet TDT v2 (English only), Parakeet Flash (beta, streaming English).                                                                                                                                                                                                                                                                                                                                                                                                             |
| M-3 | **Apple Speech** (on-device, macOS 26+).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| M-4 | Each model shows its speed and accuracy, and whether it is cloud, downloaded, or needs downloading.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| M-5 | Local models are downloaded on demand from within the app, with progress, and can be deleted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| M-6 | Post-processing (the LLM formatting pass) has local and remote providers: Groq (remote, default), Apple Intelligence (on-device, macOS 26+), and any OpenAI-compatible endpoint (local Ollama / LM Studio, or a remote service), plus Off. Any transcription model combines with any post-processing provider, so a fully offline setup is possible. Apple Intelligence rejects any request containing Hindi, so it gets only the English few-shot examples; Hindi or Hinglish dictation falls back to the deterministic text with a reason naming Groq or Custom. |
| M-7 | The Groq API key is entered in Settings where the user chooses the voice model; one key serves Groq transcription and Groq post-processing.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| M-8 | Inspired by Wispr Flow and FluidVoice, but simpler: few choices, sensible defaults, great user experience.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

## 5. Screens

| ID   | Requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| U-1  | **Home / Dictation history**: every dictation with time, app, text; copy, retry (failed), delete, search. Each row names, in fine print on the right, the model that transcribed it and, when a formatting pass produced the text, the model that formatted it, each followed by its stage's duration in brackets to one decimal (`Groq Whisper (0.3s)`, `qwen3.8-27b (1.2s)`). A skipped formatting pass shows one `Unformatted` badge whose tooltip gives the reason.                                                                                                                                                                                         |
| U-2  | **Insights**, simple and functional, in two tabs. **Personal**: words per minute, total words, words per dictation, fixes made (dictionary fixes, words corrected), streak and daily activity heatmap with active days, best day and words per active day, words by local hour of day with the most active hour, personal bests (longest dictation, biggest day by words, busiest day by dictations), usage by kind of app. **Models**: for each transcription model and each formatting model, runs, average, fastest and slowest duration, and speed (× real time for transcription, words per second for formatting), over dictations whose stage was timed. |
| U-3  | **Dictionary**: personal dictionary only, no team mode. Add, edit, delete words and replacement rules; import from a vocabulary text file. Adding keeps the dialog open: Enter adds the entry, confirms it inline, clears the fields and returns focus to the first field so the next word can be typed straight away; Done closes it. Editing an entry closes the dialog on save. Each row shows the day it was added; the Entry and Added column heads sort the list by name (A to Z first) or date added (newest first), a second click reverses it, and the chosen order persists across restarts.                                                          |
| U-4  | **Style**: exactly two modes, **Formal** (caps + punctuation) and **Casual** (caps + less punctuation). One mode applies to everything — emails, messages, all apps.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| U-5  | **Settings → General**: the two dictation shortcuts and the edit shortcut (ED-2), microphone, dictation languages. |
| U-6  | **Settings → System**: launch at login, show the floating bar at all times, show app in Dock, dictation sounds on/off with 3–4 sound options, mute all audio while dictating; Updates: running version, last check, check now, automatic checks on/off, and the upgrade command when a newer release is out.                                                                                                                                                                                                                                                                                                                                                    |
| U-7  | **Settings → Models**: engine/model selection, downloads, Groq API key, AI formatting toggle.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| U-8  | **Settings → Data**: database location.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| U-9  | While recording, a small floating pill shows (icon + live level bars) on the screen under the cursor, in every Space and over full-screen apps. After a paste it shows the start of the pasted text for 4 s instead of a word count: one line, at most 80 characters cut at a word boundary, then `...`. Showing or hiding it never activates Simple Voice: the app the dictation started in stays frontmost and keeps keyboard focus. |
| U-10 | **Settings → General → Appearance**: theme is System (follows macOS, live), Light or Dark; it applies instantly to every window surface including the title bar, persists across restarts, and the app never flashes the other theme on launch. A sun/moon icon in the window's top-right corner switches between light and dark in one click (from System it pins the opposite of what is showing).                                                                                                                                                                                                                                                            |
| U-11 | Settings are pages of the main window, not a dialog: each section (General, System, Models, Permissions, Data) is listed under a Settings group at the bottom of the sidebar and opens in the content area. The Permissions entry carries an alert dot while a required permission is missing.                                                                                                                                                                                                                                                                                                                                                                  |
| U-12 | The status card at the bottom of the sidebar shows the dictation state and both the hold-to-speak and toggle shortcuts, always matching the current settings (a newly recorded shortcut appears immediately). Hovering or focusing the card reveals a settings button that opens General settings, where the shortcuts are recorded.                                                                                                                                                                                                                                                                                                                            |
| U-13 | Launch at login registers Simple Voice as a macOS login item, listed under System Settings → General → Login Items → Open at Login. The toggle always shows what macOS has: removing the app there switches it off. A launch at login starts quietly in the menu bar without opening the main window.                                                                                                                                                                                                                                                                                                                                                           |

## 6. Permissions

| ID  | Requirement                                                                                                                                                                                                                                                                                                                         |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| X-1 | Microphone, Accessibility (to paste), and Speech Recognition (Apple Speech) permissions are requested properly, with an onboarding flow on first launch and a Permissions section in Settings showing live status and a way to grant or open System Settings.                                                                       |
| X-2 | The app never fails silently when a permission is missing: it says which permission is needed and how to grant it.                                                                                                                                                                                                                  |
| X-3 | While Accessibility access is missing, a warning that cannot be dismissed stays pinned above every page: text is only copied, not pasted (and the Fn shortcut does nothing when Fn is configured), with the action that grants access. It disappears by itself within seconds of access being granted, without relaunching the app. |
| X-4 | Permission grants survive upgrades: releases are signed with the same certificate every time (never ad hoc), so macOS matches an earlier grant to the new version and users are not asked to grant again.                                                                                                                           |

## 7. Engineering quality

| ID  | Requirement                                                                                                                                                                                                             |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Q-1 | `.editorconfig` with 4-space indentation everywhere.                                                                                                                                                                    |
| Q-2 | Quality gates like the `moneytrace` and `kalki` repositories: git hooks (pre-commit, commit-msg, pre-push), guard scripts, `just` recipes, CI.                                                                          |
| Q-3 | Rust: `unsafe` code is forbidden in every crate; strict clippy (no `unwrap`/`expect`/`panic`, no lint suppressions, no `todo!`).                                                                                        |
| Q-4 | Compile-time checked SQL via sqlx; runtime SQL strings are banned by a guard.                                                                                                                                           |
| Q-5 | Code is organized as a Cargo workspace split into crates by responsibility (`sv-domain`, `sv-storage`, `sv-text`, `sv-cloud`, `sv-audio`, `sv-engine`, app) and, inside each crate and the UI, vertically by feature.   |
| Q-6 | Rust code that needs `unsafe` (Objective-C / C APIs) lives in the Swift engine helper instead, so every Rust crate forbids `unsafe`.                                                                                    |
| Q-7 | This requirements document is kept up to date as requirements are added or changed.                                                                                                                                     |
| Q-8 | `just build` produces a sealed, ad-hoc signed bundle (entitlements and hardened runtime applied) when no Developer ID is configured, so microphone access works in local builds; `APPLE_SIGNING_IDENTITY` overrides it. |
| Q-9 | `just dev` checks query macros against a freshly migrated development database, so editing a query never breaks the dev loop before the offline cache is regenerated.                                                   |

## 8. Edit mode

| ID   | Requirement |
| ---- | ----------- |
| ED-1 | Edit mode rewrites the text selected in any app's input (a Gmail compose box or a browser search field included) by a spoken instruction: select the text, hold the edit shortcut, say how to change it ("make this more formal", "turn this into bullets"), release. The edited text replaces the selection in place; Cmd+Z in that app undoes it. No command mode: edit mode never runs commands or controls the Mac. |
| ED-2 | The edit shortcut is its own hold-only key combination, configurable in Settings → General (default `Alt+Slash`, ⌥/) and can be turned off. It can be neither Fn nor either dictation shortcut; a stored default that collides with a dictation shortcut starts off, and when only the edit shortcut cannot be registered at launch the dictation shortcuts still are. |
| ED-3 | The selection is read when the shortcut goes down: through Accessibility where the app exposes it, otherwise by a synthetic Cmd+C after which the user's clipboard is restored. Nothing selected, a selection over 4,000 characters, or missing Accessibility access ends the edit with a message saying so, and nothing is pasted. |
| ED-4 | The instruction is transcribed with the configured voice model and applied by the configured post-processing provider (Groq, Apple Intelligence, or the custom endpoint), with the selection treated as data and never as instructions. With post-processing off, edit mode says it needs a provider. |
| ED-5 | A failed, empty, truncated or timed-out edit (6 s + 20 ms per selected word, at most 30 s) leaves the selection untouched and says why. The selection's leading and trailing whitespace is kept. |
| ED-6 | The edited text is pasted only into the app the edit started in; when another app is frontmost by then, it is copied to the clipboard and the edit is recorded as not pasted. Delivery otherwise follows D-11 and D-15. |
| ED-7 | The overlay pill marks an edit with a pen from recording to result and reads "Editing" and "Edited". History keeps each edit (the instruction, the replaced selection, the result, status and models) with an Edit badge; it is searchable by the replaced text, counts the spoken instruction's words in Insights, and is excluded from formatting-pass timings. Edits keep no audio and cannot be retried. |
